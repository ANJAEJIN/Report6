1. setboard

五目の正式ルールは15*15の欄である
16*16の2次元配列を用意した後で、
1行と1列に1〜15、A〜Oを記録しておき、ユーザの利便性を増す。

2. play gomoku（石を置き）

ユーザが入力した値に対応する欄に現在の順序のユーザの石を置く（O、X）
値の形は（A、7）のように（アルファベット、数字）の形態

3. judge end

終了判定をまだ実装いないが、2つの形態を考えてみた。
3-1。板のすべての連続する5欄に O -> 1 X -> -1 を代入して合の絶対値が5であれば終了判定
3-2。最後に置かれた石について、8つの方向の連続した5欄に O -> 1 X -> -1を代入して合意絶対値が5であれば終了判定

3-2の方法がもっとうるさいだけ演算数が少なく、はるかに高速の判定が可能である